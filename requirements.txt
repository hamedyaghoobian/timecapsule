# Base dependencies for the local CUDA training scripts in this folder.
# Install a CUDA-compatible PyTorch build for your specific image/GPU setup.

torch
transformers
datasets
accelerate
pynvml

# Optional performance dependency used when available by the training script:
# flash-attn
